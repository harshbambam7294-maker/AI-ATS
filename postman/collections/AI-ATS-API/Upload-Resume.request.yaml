$kind: http-request
name: Upload Resume
method: POST
url: 'http://localhost:5000/api/users/upload-resume'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
body:
  type: formdata
  content:
    - key: resume
      type: file
      src: ''
